trigger:
  branches:
    include:
      - main

pool:
  vmImage: "ubuntu-latest"

steps:
  # Install Node.js 22 to match App Service runtime (NODE|22-lts)
  - task: NodeTool@0
    inputs:
      versionSpec: "22.x"
    displayName: "Install Node.js 22"

  # Install dependencies (including dev deps needed for build) and build
  - script: |
      npm ci
      npm run build
    displayName: "Install dependencies and build"

  # Create a ZIP archive including node_modules and build output
  - task: ArchiveFiles@2
    inputs:
      rootFolderOrFile: "$(System.DefaultWorkingDirectory)"
      includeRootFolder: false
      archiveType: "zip"
      archiveFile: "$(Build.ArtifactStagingDirectory)/app.zip"
      replaceExistingArchive: true
      verbose: true
    displayName: "Archive build output"

  # Stop the Azure App Service to prevent file locking
  - task: AzureCLI@2
    inputs:
      azureSubscription: "AzureRM-alphaseedweb"
      scriptType: "bash"
      scriptLocation: "inlineScript"
      inlineScript: |
        az webapp stop --name alphaseedweb --resource-group alphaseedinfo
    displayName: "Stop Azure App Service (alphaseedweb)"

  # Deploy to Azure App Service using ZipDeploy
  - task: AzureWebApp@1
    inputs:
      azureSubscription: "AzureRM-alphaseedweb"
      appType: "webAppLinux"
      appName: "alphaseedweb"
      package: "$(Build.ArtifactStagingDirectory)/app.zip"
      deploymentMethod: "zipDeploy"
    displayName: "Deploy to Azure App Service (alphaseedweb)"
    env:
      SYSTEM_DEBUG: true

  # Start the Azure App Service
  - task: AzureCLI@2
    inputs:
      azureSubscription: "AzureRM-alphaseedweb"
      scriptType: "bash"
      scriptLocation: "inlineScript"
      inlineScript: |
        az webapp start --name alphaseedweb --resource-group alphaseedinfo
    displayName: "Start Azure App Service (alphaseedweb)"

  # Tail logs for debugging
  - task: AzureCLI@2
    inputs:
      azureSubscription: "AzureRM-alphaseedweb"
      scriptType: "bash"
      scriptLocation: "inlineScript"
      inlineScript: |
        az webapp log tail --name alphaseedweb --resource-group alphaseedinfo
    displayName: "Tail App Service logs"
    condition: succeededOrFailed()
